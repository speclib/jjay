# Setup Initial README

**Bean**: jjay-3uge
**Status**: proposed

## What

Create the initial README.md for jjay — an alpha-stage project that automates parallel AI agent sessions using jj workspaces, tmux, and openspec.

## Scope

The README should include:

1. **Hero image** — `artwork/hero.png`, centered at top
2. **One-liner** — what jjay is (from the tagline: "Control the flock.")
3. **The workflow it automates** — describe the manual process (spawn workspace, launch agent, test, archive, merge, cleanup) and position jjay as the automation layer
4. **Prerequisites** — jj, tmux, openspec (opinionated, these are hard dependencies)
5. **Installation** — "Coming soon" placeholder
6. **CLI shape (preview)** — rough sketch of planned commands (`spawn`, `status`, `merge`, `cleanup`)
7. **Roadmap** — high-level list of planned features
8. **Contributing** — open source from day one, keep it simple
9. **License** — reference the existing LICENSE file

## Non-goals

- No badges/shields (too early)
- No "why this exists" philosophy section (let the hero + workflow speak)
- No detailed API docs (nothing to document yet)
- No installation instructions beyond placeholder
- Not polished marketing copy — this is an alpha README that communicates intent

## Notes

- Tone: direct, technical, minimal. Match the energy of the hero image.
- The manual workflow section is key — it grounds the project in a real problem and shows what jjay will replace.
- Keep it scannable. Someone should understand what jjay does in 30 seconds.
